<!DOCTYPE html>
<html>
  <head>
    <title>Prinzical</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div id="columns">
      <div id="side">
        <% if @user %>
          <p> User <%= @user.username %> eingeloggt.</p>
          <p><%= button_to 'Logout', logout_url, method: :get %></p>
        <% end %>
        <% if @user && @user.is_admin? %>
          <ul>
            <li><%= link_to "Userverwaltung", users_path, method: :get %></li>
            <li><%= link_to "Verfügbarkeiten Verwaltung", verfugbarkeits_path, method: :get %></li>
            <li><%= link_to "Buchungen Verwaltung", buchungs_path, method: :get %></li>
          </ul>
        <% end %>
        <ul>
          <li><a href="http://www....">Home</a></li>
          <li><a href="http://www..../faq">Questions</a></li>
          <li><%= link_to "Glizzi", "https://www.glizzi.com", target: "_blank" %></li>
          <li><a href="http://www..../contact">Contact</a></li>
        </ul>
        <p>
          <span class="date"><%= Time.new %></span>
        </p>
      </div>
      <div id="main">
        <%= yield %>
      </div>
    </div>
  </body>
</html>