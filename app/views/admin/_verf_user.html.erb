<div class="person"><%= "Nutzer: #{mitarbeiter.username}" %><br/>
  <%= "Name: #{mitarbeiter.name}" %></div>
<% verfugbarkeiten.each do |verf| %>
  <div class="verfügbarkeit">
    <% if verf[:potential] %>
      <% then %>
      <div class="verfügbar_buchung">
      <% else %>
        <div class="verfügbar">
        <% end %>
        <%= print_verfugbar_zeiten verf[:verfügbar] %></div>
      <div class="abwesend"><%= print_verfugbar_zeiten verf[:abwesend] %></div>
      <div class="fragen"><%= print_verfugbar_zeiten verf[:fragen] %></div>
    </div>
  <% end %>